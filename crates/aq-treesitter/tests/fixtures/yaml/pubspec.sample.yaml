# Synthetic pubspec.yaml-shaped fixture for aq-treesitter YAML grammar tests.
# Name chosen to be unmistakable as a test fixture — not a real Flutter package.

name: aq_yaml_fixture
description: Synthetic fixture for aq-treesitter YAML grammar tests.
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.0.0
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.24.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/fonts/
