[package]
name = "tq-parse"
version = "0.2.0"  # bumped from workspace 0.1.0: suite population + --record surface
edition.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
regex = { workspace = true }
