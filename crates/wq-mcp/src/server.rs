//! JSON-RPC 2.0 stdio serve loop — wired in wq-5.2 GREEN.

/// Runs the blocking stdio server loop (never returns in normal operation).
pub fn serve() -> std::io::Result<()> {
    todo!("wq-5.2 GREEN")
}
